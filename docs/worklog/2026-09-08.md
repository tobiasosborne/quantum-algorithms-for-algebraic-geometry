# Worklog — 2026-09-08

The earlier timed round was saved in commit12b38fa with the independently
reviewed fixed-source generator-selection results C-374--C-381. This entry
records the subsequent classical-baseline follow-up, not another extension of
the expired overnight deadline.

## User direction and scope

The user requested investigation of the best classical methods for one parked
geometric problem, with interest in NP-intersect-coNP and very large classical
costs. The selected problem was deciding whether a smooth quartic K3 surface
over Q has geometric Picard rank greater than one. The user then requested an
explicit integer-programming encoding. Finally, the user made NP-completeness
a rejection criterion and instructed stop, handoff/worklog update, commit and
push. Research stopped at that instruction; only finalization followed.

## Saved work

- Primary-source review of Poonen--Testa--van Luijk's unconditional K3 NS
  algorithm, Charles's specialization analysis, Lairez--Sertöz periods and
  separation bounds, modern Picard–Lefschetz periods, and Costa--Sertöz
  crystalline obstruction. Computability, practical runtimes and bit-complexity
  hypotheses are kept separate. Software documentation was inspected; the
  published large computations were not rerun.
- A direct reduction of bounded, separated period relations to fixed-variable
  integer programming. It does not supply the missing height, separation or
  period-generation bounds for an arbitrary quartic input.
- An explicit quadratic encoding of Boolean feasibility whose projective
  surface scheme has Picard rank equal to one plus the number of solutions.
  It preserves smoothness and projectivity but not connectedness, irreducibility
  or a fixed quartic K3 embedding. Under the final user criterion, it is a
  negative control, not a quantum-algorithm candidate.
- Standard BQP simulation limits and the EXPSPACE ideal-membership boundary,
  distinguishing true lower bounds from the cost of one classical method.
- Precise NP-intersect-coNP certificate obligations, with no unsupported
  membership, hardness or quantum-advantage claim for connected quartic K3s.

Files: definitions/picard-classical-r15.md;
argument/bounded-period-relations-r15.md;
argument/boolean-picard-encoding-r15.md;
scouting/picard-classical-r15.md; refs/picard-classical-r15.md.
C-382--C-385 are registered as SKETCH. No independent agent audit or upward
promotion was performed in this follow-up.

## Validation and closure

The new exact checker passed12588 finite checks. All three mutations failed
as required: missing dummy-point constraints, incomplete Segre chart coverage,
and admitting the zero relation. These tests support finite instances, not
uniform complexity claims. Commands are recorded in checkers/MUTATIONS.md.
The mandatory report commit hook rebuilds the offline artifact, validates source
coverage/reproducibility, and runs browser and isolated Git-index tests.
The source ledger and report atlas include the new work. Tracking uses Beads:
qaag-d8j for the review and qaag-uqj for the encoding. Remaining independent
review and certificate-size questions are retained as qaag-w1p and qaag-255. The final
workflow commits all changes, pushes Git and Beads, verifies a clean tree,
and stops; no further exploration is authorized by this session.
