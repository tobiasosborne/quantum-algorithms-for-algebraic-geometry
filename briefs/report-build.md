# Build the self-contained report

Root delegates the report build pipeline to you. Read briefs/report-html.md and `bd show qaag-dx0` only if necessary (avoid bd calls while root writes tracker). Your writable files are reports/build_report.py, reports/research-report.html, reports/package.json, reports/package-lock.json and reports/README.md. Root owns claims/PRD/HANDOFF; HTML agent owns report.template.html. Do not spawn agents.

Implement a reproducible Python3 build pipeline using available markdown and bs4. All source Markdown under scouting/, argument/, verdicts/, definitions/, seed report, claims/CLAIMS.md plus checker README/MUTATIONS and scientific references should be embedded in the searchable source library. Exclude operational/tool logs and briefs except any explicitly cited by route inventories (prefer replace such link with appropriate HANDOFF/source if justified). Full 69-route inventories already exist. Parse ACTUAL claim IDs and statuses from CLAIMS.md (root is now adding C-361..364; refresh final build after they land). Parse whole claim entries even early formatting differs. Preserve mathematics during Markdown rendering, including \(...\), \[...\], $$...$$ and appropriate $...$; do not alter code fences/inline code. Sanitize source HTML, strip scripts/event handlers/iframe and runtime remote media loads. Inline actual local images or replace with explicit link/caption. Escape < in JSON script. Existing HTML dialog resolves relative source links from current document; both source id/path indexed. main HTML is read from template and replace REPORT_DATA markers and KaTeX slots. Include glossary, grounded remaining questions, primary references from verified repository records. Article should need no external runtime request, no CSS/JS/font fetch. External citations are explicit-click links.

KaTeX0.16.22 and Playwright1.55.0 are installed at /tmp/qaag-report-tools/node_modules; use CLI path override or discover local reports/node_modules too. Embed KaTeX CSS and JS/auto-render, encode WOFF2 fonts as data URIs (avoid duplicating all font formats). Include proper KaTeX license in artifact/build sources. Node24.11.1. Python3 command, not python. Do not fetch more scientific content: this is a report of documented work. No unsupported novelty claims. Include all claims and all scientific docs; core article already exists. Validate route/claim/source references and fail nonzero if final build missing required docs or IDs, but allow temporary --preview while root integrates. Use bounded operations. No test files under checkers/ (different checker laws). You can add a build validation mode if meaningful.

Notify root on first full build; do not wait for perfect polishing. Root browser-tests. Keep README concise: dependencies, one build command, fully offline artifact, snapshot counts and limitations. Do not include markdown TODOs or task tracker. Make report self-contained rather than requiring running a server. Build pipeline should not alter canonical sources. Commit is root's job.
